diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/ClusteredYamapViewManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/ClusteredYamapViewManager.kt
index 54d8a0f..89dfa2a 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/ClusteredYamapViewManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/ClusteredYamapViewManager.kt
@@ -85,6 +85,7 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
                 )
             )
             .build()
+            .toMutableMap()
     }
 
     override fun getCommandsMap(): Map<String, Int>? {
@@ -124,11 +125,11 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
 
             "fitAllMarkers" -> fitAllMarkers(view)
             "fitMarkers" -> if (args != null) {
-                fitMarkers(view, args.getArray(0))
+                args.getArray(0)?.let { fitMarkers(view, it) }
             }
 
             "findRoutes" -> if (args != null) {
-                findRoutes(view, args.getArray(0), args.getArray(1), args.getString(2))
+                findRoutes(view, args.getArray(0), args.getArray(1), args.getString(2) ?: "")
             }
 
             "setZoom" -> if (args != null) {
@@ -140,11 +141,11 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
             }
 
             "getCameraPosition" -> if (args != null) {
-                view.emitCameraPositionToJS(args.getString(0))
+                args.getString(0)?.let { view.emitCameraPositionToJS(it) }
             }
 
             "getVisibleRegion" -> if (args != null) {
-                view.emitVisibleRegionToJS(args.getString(0))
+                args.getString(0)?.let { view.emitVisibleRegionToJS(it) }
             }
 
             "setTrafficVisible" -> if (args != null) {
@@ -152,11 +153,15 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
             }
 
             "getScreenPoints" -> if (args != null) {
-                view.emitWorldToScreenPoints(args.getArray(0), args.getString(1))
+                val arr = args.getArray(0)
+                val id = args.getString(1)
+                if (arr != null) view.emitWorldToScreenPoints(arr, id)
             }
 
             "getWorldPoints" -> if (args != null) {
-                view.emitScreenToWorldPoints(args.getArray(0), args.getString(1))
+                val arr = args.getArray(0)
+                val id = args.getString(1)
+                if (arr != null) view.emitScreenToWorldPoints(arr, id)
             }
 
             else -> throw IllegalArgumentException(
@@ -171,7 +176,8 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
 
     @ReactProp(name = "clusteredMarkers")
     fun setClusteredMarkers(view: View, points: ReadableArray) {
-        castToYaMapView(view).setClusteredMarkers(points.toArrayList())
+        @Suppress("UNCHECKED_CAST")
+        castToYaMapView(view).setClusteredMarkers(points.toArrayList() as ArrayList<Any>)
     }
 
     @ReactProp(name = "clusterColor")
@@ -243,7 +249,7 @@ class ClusteredYamapViewManager internal constructor() : ViewGroupManager<Cluste
             val vehicles = ArrayList<String>()
             if (jsVehicles != null) {
                 for (i in 0 until jsVehicles.size()) {
-                    vehicles.add(jsVehicles.getString(i))
+                    jsVehicles.getString(i)?.let { vehicles.add(it) }
                 }
             }
             castToYaMapView(view).findRoutes(points, vehicles, id)
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapCircleManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapCircleManager.kt
index 85be269..8769db6 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapCircleManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapCircleManager.kt
@@ -23,6 +23,7 @@ class YamapCircleManager internal constructor() : ViewGroupManager<YamapCircle>(
     override fun getExportedCustomBubblingEventTypeConstants(): MutableMap<String, Any>? {
         return MapBuilder.builder<String, Any>()
             .build()
+            .toMutableMap()
     }
 
     @Nonnull
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapMarkerManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapMarkerManager.kt
index e32d22c..6dbbe37 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapMarkerManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapMarkerManager.kt
@@ -26,6 +26,7 @@ class YamapMarkerManager internal constructor() : ViewGroupManager<YamapMarker>(
     override fun getExportedCustomBubblingEventTypeConstants(): MutableMap<String, Any>? {
         return MapBuilder.builder<String, Any>()
             .build()
+            .toMutableMap()
     }
 
     private fun castToMarkerView(view: View): YamapMarker {
@@ -107,7 +108,7 @@ class YamapMarkerManager internal constructor() : ViewGroupManager<YamapMarker>(
     ) {
         when (commandType) {
             "animatedMoveTo" -> {
-                val markerPoint = args!!.getMap(0)
+                val markerPoint = args!!.getMap(0) ?: return
                 val moveDuration = args.getInt(1)
                 val lon = markerPoint.getDouble("lon").toFloat()
                 val lat = markerPoint.getDouble("lat").toFloat()
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolygonManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolygonManager.kt
index cf14b19..bda7ff7 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolygonManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolygonManager.kt
@@ -24,6 +24,7 @@ class YamapPolygonManager internal constructor() : ViewGroupManager<YamapPolygon
     override fun getExportedCustomBubblingEventTypeConstants(): MutableMap<String, Any>? {
         return MapBuilder.builder<String, Any>()
             .build()
+            .toMutableMap()
     }
 
     private fun castToPolygonView(view: View): YamapPolygon {
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolylineManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolylineManager.kt
index 8be99c8..97776e8 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolylineManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapPolylineManager.kt
@@ -24,6 +24,7 @@ class YamapPolylineManager internal constructor() : ViewGroupManager<YamapPolyli
     override fun getExportedCustomBubblingEventTypeConstants(): MutableMap<String, Any>? {
         return MapBuilder.builder<String, Any>()
             .build()
+            .toMutableMap()
     }
 
     private fun castToPolylineView(view: View): YamapPolyline {
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapViewManager.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapViewManager.kt
index 8b05905..833428d 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapViewManager.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/YamapViewManager.kt
@@ -85,6 +85,7 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
                 )
             )
             .build()
+            .toMutableMap()
     }
 
     override fun getCommandsMap(): Map<String, Int>? {
@@ -124,11 +125,11 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
 
             "fitAllMarkers" -> fitAllMarkers(view)
             "fitMarkers" -> if (args != null) {
-                fitMarkers(view, args.getArray(0))
+                args.getArray(0)?.let { fitMarkers(view, it) }
             }
 
             "findRoutes" -> if (args != null) {
-                findRoutes(view, args.getArray(0), args.getArray(1), args.getString(2))
+                findRoutes(view, args.getArray(0), args.getArray(1), args.getString(2) ?: "")
             }
 
             "setZoom" -> if (args != null) {
@@ -140,11 +141,11 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
             }
 
             "getCameraPosition" -> if (args != null) {
-                view.emitCameraPositionToJS(args.getString(0))
+                args.getString(0)?.let { view.emitCameraPositionToJS(it) }
             }
 
             "getVisibleRegion" -> if (args != null) {
-                view.emitVisibleRegionToJS(args.getString(0))
+                args.getString(0)?.let { view.emitVisibleRegionToJS(it) }
             }
 
             "setTrafficVisible" -> if (args != null) {
@@ -152,11 +153,15 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
             }
 
             "getScreenPoints" -> if (args != null) {
-                view.emitWorldToScreenPoints(args.getArray(0), args.getString(1))
+                val arr = args.getArray(0)
+                val id = args.getString(1)
+                if (arr != null) view.emitWorldToScreenPoints(arr, id)
             }
 
             "getWorldPoints" -> if (args != null) {
-                view.emitScreenToWorldPoints(args.getArray(0), args.getString(1))
+                val arr = args.getArray(0)
+                val id = args.getString(1)
+                if (arr != null) view.emitScreenToWorldPoints(arr, id)
             }
 
             else -> throw IllegalArgumentException(
@@ -237,7 +242,7 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
 
             if (jsVehicles != null) {
                 for (i in 0 until jsVehicles.size()) {
-                    vehicles.add(jsVehicles.getString(i))
+                    jsVehicles.getString(i)?.let { vehicles.add(it) }
                 }
             }
 
@@ -248,9 +253,7 @@ class YamapViewManager internal constructor() : ViewGroupManager<YamapView>() {
     // PROPS
     @ReactProp(name = "userLocationIcon")
     fun setUserLocationIcon(view: View, icon: String?) {
-        if (icon != null) {
-            castToYaMapView(view).setUserLocationIcon(icon)
-        }
+        icon?.let { castToYaMapView(view).setUserLocationIcon(it) }
     }
 
     @ReactProp(name = "userLocationIconScale")
diff --git a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/view/YamapView.kt b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/view/YamapView.kt
index 48c1cdf..0e94901 100644
--- a/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/view/YamapView.kt
+++ b/node_modules/react-native-yamap/android/src/main/java/ru/vvdev/yamap/view/YamapView.kt
@@ -225,7 +225,7 @@ open class YamapView(context: Context?) : MapView(context), UserLocationObjectLi
         val screenPoints = Arguments.createArray()
 
         for (i in 0 until worldPoints.size()) {
-            val p = worldPoints.getMap(i)
+            val p = worldPoints.getMap(i) ?: continue
             val worldPoint = Point(p.getDouble("lat"), p.getDouble("lon"))
             val screenPoint = mapWindow.worldToScreen(worldPoint)
             screenPoints.pushMap(screenPointToJSON(screenPoint))
@@ -244,7 +244,7 @@ open class YamapView(context: Context?) : MapView(context), UserLocationObjectLi
         val worldPoints = Arguments.createArray()
 
         for (i in 0 until screenPoints.size()) {
-            val p = screenPoints.getMap(i)
+            val p = screenPoints.getMap(i) ?: continue
             val screenPoint = ScreenPoint(p.getDouble("x").toFloat(), p.getDouble("y").toFloat())
             val worldPoint = mapWindow.screenToWorld(screenPoint)
             worldPoints.pushMap(worldPointToJSON(worldPoint))
@@ -685,7 +685,8 @@ open class YamapView(context: Context?) : MapView(context), UserLocationObjectLi
         val wTransports = Arguments.createMap()
 
         for ((key, value) in transports) {
-            wTransports.putArray(key, Arguments.fromList(value))
+            @Suppress("UNCHECKED_CAST")
+            wTransports.putArray(key, Arguments.fromList((value ?: emptyList<Any>()) as List<Any?>))
         }
 
         routeMetadata.putMap("transports", wTransports)
